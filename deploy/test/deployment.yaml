apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: zhishuyun-data-frontend
  name: zhishuyun-data-frontend
  namespace: zhishuyun-test
spec:
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: zhishuyun-data-frontend
  template:
    metadata:
      labels:
        app: zhishuyun-data-frontend
    spec:
      containers:
        - image: ghcr.io/zhishuyun/zhishuyun-data-frontend:${TAG}
          name: zhishuyun-data-frontend
          ports:
            - containerPort: 80
          resources:
            requests:
              memory: '200Mi'
              cpu: '100m'
            limits:
              memory: '512Mi'
              cpu: '200m'
      imagePullSecrets:
        - name: docker-registry
      restartPolicy: Always
